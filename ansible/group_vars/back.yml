---
# Back est en subnet privé : on rebondit via Front (bastion) pour la connexion SSH.
ansible_ssh_common_args: >-
  -o ProxyCommand="ssh -W %h:%p -q ubuntu@{{ bastion_host }} -i {{ ansible_ssh_private_key_file }}"